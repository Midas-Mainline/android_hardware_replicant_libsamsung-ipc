diff --git a/Android.mk b/Android.mk
index 8e79c8f..b2c6132 100644
--- a/Android.mk
+++ b/Android.mk
@@ -27,9 +27,10 @@ ifeq ($(TARGET_DEVICE),crespo)
 endif
 
 ifeq ($(TARGET_DEVICE),galaxysmtd)
-	device_files := samsung-ipc/device/$(TARGET_DEVICE)/$(TARGET_DEVICE)_nv_data.c
-	LOCAL_CFLAGS += -Iexternal/openssl/include
-	LOCAL_LDFLAGS += -lcrypto
+	LOCAL_CFLAGS += -DDEVICE_CRESPO
+endif
+
+ifeq ($(TARGET_DEVICE),galaxytab)
 	LOCAL_CFLAGS += -DDEVICE_CRESPO
 endif
 
@@ -62,9 +63,15 @@ modemctrl_files := tools/modemctrl.c
 ifeq ($(TARGET_DEVICE),crespo)
 	LOCAL_CFLAGS += -DDEVICE_CRESPO
 endif
+
 ifeq ($(TARGET_DEVICE),galaxysmtd)
 	LOCAL_CFLAGS += -DDEVICE_CRESPO
 endif
+
+ifeq ($(TARGET_DEVICE),galaxytab)
+	LOCAL_CFLAGS += -DDEVICE_CRESPO
+endif
+
 ifeq ($(TARGET_DEVICE),h1)
 	LOCAL_CFLAGS += -DDEVICE_H1
 endif
diff --git a/include/gprs.h b/include/gprs.h
index efe4324..67f70d3 100644
--- a/include/gprs.h
+++ b/include/gprs.h
@@ -42,6 +42,7 @@
 #define IPC_GPRS_CALL_STATUS_TYPE_OFF       0x3
 
 #define IPC_GPRS_ERROR_UNAVAILABLE          0x94
+#define IPC_GPRS_ERROR_NOT_SUBSCRIBED       0x0B
 
 struct ipc_gprs_define_pdp_context {
     unsigned char unk0[3];
@@ -91,6 +92,7 @@ struct ipc_gprs_current_session_data_counter {
 };
 
 void ipc_gprs_pdp_context_setup(struct ipc_gprs_pdp_context *message, char *username, char *password);
+//void ipc_gprs_define_pdp_context_setup(struct ipc_gprs_define_pdp_context *message, char *apn);
 
 #endif
 
diff --git a/include/types.h b/include/types.h
index 0211042..2a39af7 100644
--- a/include/types.h
+++ b/include/types.h
@@ -73,18 +73,6 @@
 #define IPC_PB_ENTRY_INFO                           0x0604
 #define IPC_PB_CAPABILITY_INFO                      0x0605
 
-/* SS */
-#define IPC_SS_WAITING                              0x0C01
-#define IPC_SS_CLI                                  0x0C02
-#define IPC_SS_BARRING                              0x0C03
-#define IPC_SS_BARRING_PW                           0x0C04
-#define IPC_SS_FORWARDING                           0x0C05
-#define IPC_SS_INFO                                 0x0C06
-#define IPC_SS_MANAGE_CALL                          0x0C07
-#define IPC_SS_USSD                                 0x0C08
-#define IPC_SS_AOC                                  0x0C09
-#define IPC_SS_RELEASE_COMPLETE                     0x0C0A
-
 /* Sat */
 #define IPC_SAT_PROFILE_DOWNLOAD                    0x0E01
 #define IPC_SAT_ENVELOPE_CMD                        0x0E02
